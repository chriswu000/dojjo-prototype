<h1>Editing entry</h1>

<%= link_to 'Back', user_path(current_user) %>
<%= render 'entries/form', :entry => @entry %>
<br />

<%= render @entry.items %>

<%= link_to 'Back', user_path(current_user) %>
